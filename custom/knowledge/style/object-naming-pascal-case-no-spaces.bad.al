// Spaces in name — harder to reference, inconsistent.
codeunit 50130 "Synchronize Company Vehicle_PTE"
{
}

// Abbreviated verb and noun — meaning degrades over time.
codeunit 50131 "Sync Veh Grp_PTE"
{
}

// Type name embedded in object name — redundant with the object declaration.
codeunit 50132 "SyncVehicleGroupCodeunit_PTE"
{
}

// Verb shortened first instead of noun — wrong priority when trimming for length.
codeunit 50133 "SyncPurchaseWarehouseDocument_PTE"
{
}
